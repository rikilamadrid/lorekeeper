/**
 * The agent demo's question, asked the three ways brand/demo/agent-demo.mjs
 * asks it. A test holds the two lists together.
 */
export const DEMO_WORDINGS = [
  'nightly export failed',
  'run stopped partway and reported success',
  'consumer lease renewal committed offset',
] as const;
