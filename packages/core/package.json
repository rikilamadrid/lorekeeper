{
  "name": "@lorekeeper/core",
  "version": "0.0.0",
  "private": true,
  "description": "Frontmatter contracts, validation, and managed manifest format.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "vitest run"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
